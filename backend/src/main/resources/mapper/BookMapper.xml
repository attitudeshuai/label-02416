<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.BookMapper">

    <select id="findById" resultType="com.library.entity.Book">
        SELECT * FROM book WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book (isbn, title, author, publisher, publish_date, category, price, stock, description, cover_image, status)
        VALUES (#{isbn}, #{title}, #{author}, #{publisher}, #{publishDate}, #{category}, #{price}, #{stock}, #{description}, #{coverImage}, #{status})
    </insert>

    <update id="update">
        UPDATE book SET isbn = #{isbn}, title = #{title}, author = #{author}, publisher = #{publisher},
        publish_date = #{publishDate}, category = #{category}, price = #{price}, stock = #{stock},
        description = #{description}, cover_image = #{coverImage}, status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM book WHERE id = #{id}
    </delete>

    <select id="findByCondition" resultType="com.library.entity.Book">
        SELECT * FROM book
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND author LIKE CONCAT('%', #{author}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="isbn != null and isbn != ''">
                AND isbn LIKE CONCAT('%', #{isbn}, '%')
            </if>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                     OR author LIKE CONCAT('%', #{keyword}, '%')
                     OR description LIKE CONCAT('%', #{keyword}, '%')
                     OR isbn LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByCondition" resultType="java.lang.Long">
        SELECT COUNT(*) FROM book
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND author LIKE CONCAT('%', #{author}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="isbn != null and isbn != ''">
                AND isbn LIKE CONCAT('%', #{isbn}, '%')
            </if>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                     OR author LIKE CONCAT('%', #{keyword}, '%')
                     OR description LIKE CONCAT('%', #{keyword}, '%')
                     OR isbn LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <select id="findAllCategories" resultType="java.lang.String">
        SELECT DISTINCT category FROM book WHERE category IS NOT NULL
    </select>

    <update id="decreaseStock">
        UPDATE book SET stock = stock - 1 WHERE id = #{id} AND stock > 0
    </update>

    <update id="increaseStock">
        UPDATE book SET stock = stock + 1 WHERE id = #{id}
    </update>

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM book
    </select>

    <select id="countByCategory" resultType="java.lang.Long">
        SELECT COUNT(*) FROM book WHERE category = #{category}
    </select>

    <select id="findLatest" resultType="com.library.entity.Book">
        SELECT * FROM book ORDER BY create_time DESC LIMIT 5
    </select>

</mapper>
