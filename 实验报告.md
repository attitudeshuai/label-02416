# 实验报告：Spring Boot2 整合 MyBatis 框架

## 一、实验名称

Spring Boot2 整合 MyBatis 框架——创新类图书信息管理子系统

## 二、实验目的

1. 掌握 Spring Boot2 集成 MyBatis 框架的方法
2. 掌握 Spring Boot2 简单实践应用
3. 理解前后端分离架构的设计与实现

## 三、实验环境

| 项目 | 版本/说明 |
|------|----------|
| JDK | 11 |
| Spring Boot | 2.7.18 |
| MyBatis | 2.3.1（mybatis-spring-boot-starter） |
| MySQL | 8.0 |
| Vue.js | 3.3.8 |
| Element Plus | 2.4.2 |
| Maven | 3.8.8（通过 Maven Wrapper） |
| Docker | 用于容器化部署 |

## 四、实验内容

### 4.1 系统功能概述

本系统为创新类图书信息管理子系统，实现以下功能：

1. 用户注册、登录功能（JWT Token 认证 + BCrypt 密码加密）
2. 管理员用户：图书信息的增、删、改、查，文件上传（封面图片），分类管理
3. 普通用户：图书信息查询（支持单条件查询与多条件查询），图书借阅与归还
4. 数据可视化统计仪表盘（ECharts 图表）

### 4.2 技术架构

采用前后端分离架构：
- 后端：Spring Boot + MyBatis + MySQL，RESTful API 设计
- 前端：Vue.js 3 + Element Plus + Axios
- 部署：Docker + Docker Compose + Nginx 反向代理

### 4.3 数据库设计

系统包含 4 张核心数据表：

1. `sys_user`（用户表）：存储用户账号、密码（BCrypt 加密）、角色等信息
2. `book`（图书表）：存储图书 ISBN、书名、作者、分类、库存等信息
3. `borrow_record`（借阅记录表）：记录用户借阅/归还的时间和状态
4. `category`（分类表）：管理图书分类信息

详细建表语句见 `backend/src/main/resources/db/init.sql`。

### 4.4 后端实现

#### 项目结构

```
com.library
├── controller/          # 控制器层（AuthController, BookController, BorrowController, CategoryController, FileController, DashboardController）
├── service/             # 服务层（UserService, BookService, BorrowService, CategoryService, FileService）
├── mapper/              # 数据访问层（MyBatis Mapper 接口）
├── entity/              # 实体类（User, Book, BorrowRecord, Category）
├── dto/                 # 数据传输对象（LoginRequest, RegisterRequest, BookRequest, BookQueryRequest, CategoryRequest）
├── common/              # 公共类（Result 统一响应, PageResult 分页, GlobalExceptionHandler 全局异常处理, ForbiddenException）
├── config/              # 配置类（WebConfig CORS配置, JwtInterceptor JWT拦截器, SecurityConfig 安全配置）
└── util/                # 工具类（JwtUtil JWT令牌工具）
```

#### Spring Boot 整合 MyBatis 关键配置

`application.yml` 中配置 MyBatis：
```yaml
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.library.entity
  configuration:
    map-underscore-to-camel-case: true   # 自动驼峰映射
```

Mapper 接口使用 `@Mapper` 注解，支持注解 SQL 和 XML 映射两种方式：
- 简单 CRUD 使用 `@Select`、`@Insert`、`@Update`、`@Delete` 注解
- 复杂动态查询使用 XML 映射文件（如 `BookMapper.xml` 中的条件查询）

#### 参数校验

使用 `spring-boot-starter-validation` 实现 Bean Validation：
- DTO 类使用 `@NotBlank`、`@Size`、`@Min`、`@Max`、`@DecimalMin` 等注解
- Controller 方法参数使用 `@Valid` 触发校验
- `GlobalExceptionHandler` 统一捕获 `MethodArgumentNotValidException`，返回 HTTP 400 + 标准错误格式

#### 异常处理

全局异常处理器 `GlobalExceptionHandler` 统一处理所有异常，返回对应 HTTP 状态码：
- 参数校验失败 → HTTP 400
- 权限不足（ForbiddenException）→ HTTP 403
- 文件操作异常 → HTTP 404
- 业务异常 → HTTP 400
- 系统异常 → HTTP 500

### 4.5 前端实现

#### 页面模块
- 登录/注册页面：表单校验 + JWT Token 存储
- 图书管理页面：支持综合搜索（关键词模糊匹配）和精确搜索（按书名/作者/ISBN 单独查询），分页展示
- 借阅管理页面：借阅/归还操作，借阅记录查看
- 分类管理页面：分类的增删改查（管理员）
- 仪表盘页面：ECharts 数据可视化统计

#### 查询功能说明
- 单条件查询：可按书名、作者、ISBN、分类分别进行精确搜索
- 多条件查询：综合搜索模式下，关键词同时匹配书名、作者、ISBN、简介

### 4.6 安全设计

1. JWT Token 认证：登录成功后签发 Token，后续请求通过 `Authorization` 头携带
2. BCrypt 密码加密：用户密码使用 BCrypt 算法加密存储
3. 权限控制：管理员操作（增删改、文件上传）通过角色校验，权限不足返回 HTTP 403
4. 文件上传安全：路径穿越防护、文件名随机化（UUID）
5. 敏感配置外部化：数据库密码等通过环境变量注入

### 4.7 部署方案

提供 Docker Compose 一键部署：
```bash
docker-compose up --build -d
```
自动启动 MySQL、后端、前端三个服务，init.sql 自动初始化数据库。

## 五、实验结果

系统成功实现了图书信息管理子系统的全部功能：
1. 用户注册与登录功能正常，JWT 认证机制有效
2. 管理员可进行图书的增、删、改、查及封面上传
3. 普通用户可进行单条件查询和多条件查询
4. 借阅与归还功能正常，库存自动增减
5. 数据统计仪表盘正确展示各项指标
6. Docker 一键部署验证通过

## 六、实验总结

通过本次实验，掌握了以下技术要点：
1. Spring Boot 整合 MyBatis 的配置方法，包括注解 SQL 和 XML 映射两种方式
2. 使用 Bean Validation 实现参数校验，配合全局异常处理器统一错误返回
3. JWT Token 认证机制的实现，包括签发、验证、拦截器配置
4. 前后端分离架构下的 RESTful API 设计与跨域配置
5. Docker 容器化部署的实践，包括多阶段构建和服务编排
